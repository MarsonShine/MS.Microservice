# Laboratory domain

Local identity/password examples, orders, log aggregates and the associated teaching models live in this application-specific assembly. Reusable domain contracts remain in Domain.Primitives. Production reference profiles use a separate domain model linked to external identity.
